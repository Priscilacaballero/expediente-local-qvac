import type { ApiFinding, ApiDocument } from "../api-client.js";
export function EvidencePanel({ finding, documents, onClose }: { finding: ApiFinding | null; documents: ApiDocument[]; onClose: () => void }) { if (!finding) return null; const document = documents.find((item) => item.id === finding.documentId); return <aside className="evidence-panel"><button className="close-button" onClick={onClose}>×</button><span className="eyebrow">EVIDENCIA</span><h3>Fuente documental</h3><p>{finding.message}</p><div className="evidence-placeholder"><strong>{document?.filename ?? "Sin documento asociado"}</strong><br />{finding.page ? `Página ${finding.page}` : "Campo no encontrado en los documentos"}<br /><br />{finding.quote ? `“${finding.quote}”` : "La alerta se basa en la ausencia de un campo obligatorio."}</div></aside> }
