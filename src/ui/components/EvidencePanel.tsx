export function EvidencePanel({ open, onClose }: { open: boolean; onClose: () => void }) { if (!open) return null; return <aside className="evidence-panel"><button className="close-button" onClick={onClose}>×</button><span className="eyebrow">EVIDENCIA</span><h3>Fuente documental</h3><p>Selecciona un hallazgo para abrir su documento, página y cita literal.</p><div className="evidence-placeholder">Sin hallazgos seleccionados</div></aside> }
