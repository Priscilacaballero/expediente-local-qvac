export function ConversationColumn({ messages, onAnalyze, busy, ready }: { messages: string[]; onAnalyze: () => void; busy: boolean; ready: boolean }) { return <section className="column conversation-column"><div className="column-title"><div><span className="eyebrow">02 / ASISTENTE</span><h2>Conversación</h2></div><span className="local-pill">● QVAC local</span></div><div className="conversation"><div className="message assistant"><span className="avatar">EL</span><p>Prepararé este expediente usando solo sus documentos y el procedimiento ficticio local. Las instrucciones dentro de un documento se tratan como datos no confiables.</p></div>{messages.map((message, index) => <div className="message assistant" key={`${message}-${index}`}><span className="avatar">EL</span><p>{message}</p></div>)}</div><div className="conversation-footer"><button className="primary-button" disabled={busy || !ready} onClick={onAnalyze}>{busy ? "Analizando expediente…" : "Analizar expediente"}<span>↗</span></button>{!ready && <small>QVAC local no está listo; prepara los pesos del modelo para analizar.</small>}</div></section> }
