export function StatusBadge({ status }: { status: string }) { const label: Record<string, string> = { nuevo: "Nuevo", documentos_cargados: "Documentos cargados", analizando: "Analizando", requiere_datos: "Requiere datos", requiere_revision: "Requiere revisión", listo_para_revision: "Listo para revisión" }; return <span className={`status status-${status}`}>{label[status] ?? status}</span>; }
