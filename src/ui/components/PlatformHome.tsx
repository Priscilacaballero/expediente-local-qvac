import type { PlatformActivity, PlatformArchitecture, PlatformStatus, UseCaseDefinition, UseCaseId } from "../api-client.js";
const icons: Record<UseCaseId, string> = { client_guidance: "01", financial_inclusion: "02", document_review: "03", operations: "04", security_review: "05" };
export function PlatformHome({ useCases, architecture, status, activity, onSelect }: { useCases: UseCaseDefinition[]; architecture: PlatformArchitecture; status: PlatformStatus | null; activity: PlatformActivity[]; onSelect: (item: UseCaseDefinition) => void }) {
  return <section className="platform-home"><div className="platform-hero"><div><span className="eyebrow">PLATAFORMA DE INTELIGENCIA BANCARIA LOCAL</span><h1>Un centro de trabajo para decidir con más privacidad.</h1><p>Orientación, expedientes, operación y seguridad conectados por un mismo flujo: datos locales, QVAC local, evidencia trazable y revisión humana.</p></div><div className="hero-proof"><strong>QVAC local</strong><span>{status?.ready ? "Listo para inferir" : "Pendiente de preparar"}</span><small>Datos sintéticos · sin inferencia cloud</small></div></div><div className="platform-section-heading"><div><span className="eyebrow">ELIGE UN OBJETIVO</span><h2>¿Qué quieres resolver?</h2></div><span className="section-note">Cada espacio muestra su entrada, resultado y límite.</span></div><div className="use-case-grid">{useCases.map(item => <button className="use-case-card" key={item.id} onClick={() => onSelect(item)}><span className="use-case-number">{icons[item.id]}</span><span className="card-kicker">{item.audience}</span><h3>{item.title}</h3><p>{item.problem}</p><span className="use-case-result"><strong>Resultado</strong>{item.output}</span><span className="use-case-link">Abrir espacio →</span></button>)}</div><div className="platform-lower"><article className="architecture-card"><span className="card-kicker">CÓMO FUNCIONA</span><h2>La inteligencia permanece cerca del dato</h2><div className="architecture-flow"><span>Dato autorizado</span><i>→</i><span>QVAC local</span><i>→</i><span>Reglas + evidencia</span><i>→</i><span>Revisión humana</span></div><p>{architecture.inferenceBoundary}</p><p>{architecture.validation}</p><small>{architecture.pearsExtension}</small></article><article className="activity-card"><div className="card-heading"><div><span className="card-kicker">TRAZABILIDAD</span><h2>Actividad reciente</h2></div><span className="card-count">local</span></div>{activity.length ? activity.slice(0, 4).map(item => <div className="activity-row" key={item.id}><span className="activity-dot"/><div><strong>{item.result}</strong><small>{item.useCase} · {item.humanReview === "pending" ? "revisión humana pendiente" : item.humanReview}</small></div></div>) : <p className="empty-state">Selecciona un objetivo para iniciar el primer espacio de trabajo.</p>}</article></div></section>;
}
