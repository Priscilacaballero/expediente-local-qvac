import { useEffect, useState } from "react";
import { apiClient, type RiskAlert, type Transaction } from "../api-client.js";
export function SecurityMode() { const [transactions, setTransactions] = useState<Transaction[]>([]); const [alerts, setAlerts] = useState<RiskAlert[]>([]); const [error, setError] = useState(""); useEffect(() => { void Promise.all([apiClient.transactions(), apiClient.alerts()]).then(([t, a]) => { setTransactions(t); setAlerts(a); }).catch(() => setError("No se pudo cargar la bandeja local de seguridad.")); }, []); return <section className="mode-content"><div className="mode-heading"><div><span className="eyebrow">SEGURIDAD</span><h2>Bandeja de revisión humana</h2><p>Las alertas son señales explicables sobre datos sintéticos. Nunca bloquean, acusan ni deciden automáticamente.</p></div></div><article className="mode-card"><h3>Alertas locales ({alerts.length})</h3>{alerts.map(a => <div className="alert-card" key={a.id}><div><strong>{a.alertType}</strong><span className={`severity ${a.severity}`}>{a.severity}</span></div><p>{a.message}</p><small>Transacciones: {a.transactionIds.join(", ")} · Estado: revisión humana</small></div>)}{!alerts.length && <p>No hay alertas.</p>}</article><article className="mode-card"><h3>Transacciones sintéticas ({transactions.length})</h3><div className="table-wrap"><table><thead><tr><th>Cliente</th><th>Monto</th><th>Beneficiario</th><th>Canal</th><th>País</th></tr></thead><tbody>{transactions.map(t => <tr key={t.id}><td>{t.customerRef}</td><td>{t.amount} {t.currency}</td><td>{t.beneficiary}</td><td>{t.channel}</td><td>{t.country}</td></tr>)}</tbody></table></div>{error && <p className="notice">{error}</p>}</article></section>; }
