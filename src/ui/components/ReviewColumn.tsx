import type { CaseSummary } from "../api-client.js";
import { StatusBadge } from "./StatusBadge.js";
export function ReviewColumn({ selected, onReport, busy }: { selected: CaseSummary; onReport: () => void; busy: boolean }) { const items = ["Identidad y datos personales", "Ingresos y moneda", "Fuente de fondos", "Contradicciones"]; return <section className="column review-column"><div className="column-title"><div><span className="eyebrow">03 / CONTROL</span><h2>Revisión</h2></div><span className="review-count">0 hallazgos</span></div><div className="review-summary"><span className="summary-label">Estado del expediente</span><StatusBadge status={selected.status} /><p>Los hallazgos se mostrarán aquí con su regla, severidad y fuente exacta.</p></div><div className="checklist">{items.map((item) => <div className="check-row" key={item}><span className="check-empty">○</span><span>{item}</span><span className="pending">pendiente</span></div>)}</div><button className="secondary-button" disabled={busy} onClick={onReport}>Generar reporte local <span>↓</span></button><p className="fine-print">Una discrepancia significa revisión humana; no prueba fraude.</p></section> }
